# 🏛️ MKM Study 현재 구현 상태 및 개선 방안

**작성일**: 2026-01-20  
**목적**: 제안된 Nitro 아이디어들의 현재 구현 상태 확인 및 개선 방안 제시

---

## 📊 현재 구현 상태

### ✅ 이미 구현된 기능

#### 1. RPPG (원격 광혈류측정)
- **위치**: `src/components/RPPGVideoFeed.tsx`, `src/utils/rppgProcessor.ts`
- **기능**: 실시간 심박수, HRV 계산, 졸음 감지
- **상태**: ✅ 완전 구현됨

#### 2. 음성 분석 (Voice Biomarker)
- **위치**: `mkm-life`, `mkm-studio` 프로젝트에 있음
- **기능**: jitter, shimmer, pitch, energy 분석
- **상태**: ✅ 부분 구현됨 (mkm-study에는 아직 통합 안 됨)

#### 3. AI 대화 기능
- **위치**: `src/components/MKMStudyApp.tsx`, `src/utils/api.ts`
- **기능**: Web Speech API + Gemma3 API
- **상태**: ✅ 기본 구현됨 (성능 개선 필요)

#### 4. 체질 진단 (A-Code)
- **위치**: `src/components/UserProfileForm.tsx`
- **기능**: Type-A/B/C/D 진단
- **상태**: ✅ 완전 구현됨 (용어 현대화 완료)

---

## 🚀 제안된 기능들의 구현 상태

### 1. 생체 리듬 기반 '동적 학습 스케줄링' (Biological-Sync)

**현재 상태**: ⚠️ 부분 구현됨
- ✅ RPPG로 HRV/스트레스 계산 가능
- ❌ 학습 난이도 자동 조절 로직 없음
- ❌ 스트레스 높을 때 쉬운 문제로 전환 기능 없음

**구현 필요 사항**:
```typescript
// src/utils/adaptiveLearningScheduler.ts
interface LearningSchedule {
  difficulty: 'easy' | 'medium' | 'hard';
  subject: 'math' | 'english';
  reason: string; // "HRV 낮음, 스트레스 높음 → 쉬운 문제 권장"
}

function adjustLearningDifficulty(
  hrv: number,
  stressLevel: 'low' | 'normal' | 'high',
  currentDifficulty: 'easy' | 'medium' | 'hard'
): LearningSchedule {
  // HRV < 30 또는 스트레스 높음 → 쉬운 문제
  // HRV > 50, 스트레스 낮음 → 어려운 문제
}
```

---

### 2. 음성 기반 '메타인지 확신도 분석' (Voice-Confidence Check)

**현재 상태**: ⚠️ 부분 구현됨
- ✅ 음성 분석 서비스 존재 (mkm-life, mkm-studio)
- ❌ 메타인지 확신도 분석 로직 없음
- ❌ "방금 부분은 조금 불안한데?" 자동 감지 없음

**구현 필요 사항**:
```typescript
// src/utils/metaCognitionAnalyzer.ts
interface ConfidenceAnalysis {
  confidence: number; // 0-1
  uncertainty: number; // 0-1 (높을수록 불안정)
  recommendation: string; // "방금 부분은 조금 불안한데? 다시 한번 짚어볼까?"
}

function analyzeConfidence(
  jitter: number,
  shimmer: number,
  pitchVariability: number,
  pauseCount: number
): ConfidenceAnalysis {
  // jitter > 0.05, shimmer > 0.1 → 확신도 낮음
  // pitchVariability 높음 → 불안정
  // pauseCount 많음 → 머뭇거림
}
```

---

### 3. '수능 역추적(Back-mapping)' 커리큘럼 설계

**현재 상태**: ❌ 구현 안 됨
- ✅ 커리큘럼 데이터는 있음
- ❌ 중학 개념 → 수능 킬러 문항 매핑 없음
- ❌ "이 개념이 2024년 수능 수학 22번의 핵심 논리" 알림 없음

**구현 필요 사항**:
```typescript
// src/data/examBackMapping.ts
interface ExamMapping {
  middleSchoolConcept: string; // "연립방정식"
  examYear: number; // 2024
  examNumber: number; // 22
  examType: 'killer' | 'standard' | 'basic';
  connection: string; // "이 개념이 2024년 수능 수학 22번의 핵심 논리로 쓰였습니다"
}

const EXAM_MAPPINGS: ExamMapping[] = [
  {
    middleSchoolConcept: "연립방정식",
    examYear: 2024,
    examNumber: 22,
    examType: "killer",
    connection: "이 개념이 2024년 수능 수학 22번의 핵심 논리로 쓰였습니다"
  },
  // ...
];
```

---

### 4. 체질별 '페이스메이커 페르소나' 매칭

**현재 상태**: ❌ 구현 안 됨
- ✅ 체질 진단은 있음 (Type-A/B/C/D)
- ❌ 페이스메이커 페르소나 없음
- ❌ 체질별 튜터 성격/격려 방식 없음

**구현 필요 사항**:
```typescript
// src/utils/personaMatcher.ts
interface TutorPersona {
  type: 'Type-A' | 'Type-B' | 'Type-C' | 'Type-D';
  personality: 'competitive' | 'empathetic' | 'analytical' | 'supportive';
  encouragementStyle: string[];
  tone: 'energetic' | 'calm' | 'friendly' | 'gentle';
}

const PERSONA_MAP: Record<string, TutorPersona> = {
  'Type-A': {
    type: 'Type-A',
    personality: 'competitive',
    encouragementStyle: [
      '경쟁형 라이벌: "다른 친구들보다 빠르게 해결해보자!"',
      '성취욕 자극: "이 문제를 풀면 레벨업이야!"'
    ],
    tone: 'energetic'
  },
  'Type-B': {
    type: 'Type-B',
    personality: 'empathetic',
    encouragementStyle: [
      '공감형 멘토: "괜찮아, 천천히 해보자"',
      '불안 완화: "틀려도 괜찮아, 배우는 과정이야"'
    ],
    tone: 'calm'
  },
  // ...
};
```

---

## 🎯 AI 대화 성능 개선 방안

### 현재 문제점

1. **응답 속도**: 타임아웃 30초, 재시도 3회 → 느림
2. **무전기 방식**: 현재는 단방향 (음성 → 텍스트 → API → 응답 표시)
3. **실시간성 부족**: 사용자가 말하는 동안 응답 대기

### 개선 방안

#### 1. 무전기 방식 양방향 대화 (Push-to-Talk)

```typescript
// src/components/WalkieTalkieConversation.tsx
interface WalkieTalkieMode {
  isPushing: boolean; // 버튼 누르고 있는 동안만 녹음
  isProcessing: boolean; // AI 응답 생성 중
  isSpeaking: boolean; // AI 음성 출력 중
}

// 사용자: 버튼 누름 → 말함 → 버튼 놓음 → AI 응답 → 음성 출력
```

**장점**:
- 배경 소음 감소
- 명확한 대화 구분
- 실시간성 향상

#### 2. 스트리밍 응답 (Streaming Response)

```typescript
// src/utils/api.ts
export async function* answerQuestionStreaming(
  question: string,
  vectorState: Vector4D,
  subject?: 'math' | 'english'
): AsyncGenerator<string> {
  // Gemma3 스트리밍 모드 사용
  const response = await fetch(`${GEMMA3_URL}/api/generate`, {
    method: 'POST',
    body: JSON.stringify({
      model: 'llama3.2:3b',
      prompt: question,
      stream: true // 스트리밍 활성화
    })
  });

  const reader = response.body?.getReader();
  const decoder = new TextDecoder();

  while (true) {
    const { done, value } = await reader!.read();
    if (done) break;
    
    const chunk = decoder.decode(value);
    yield chunk; // 실시간으로 텍스트 전송
  }
}
```

**효과**: 응답 첫 부분을 즉시 표시 (체감 속도 향상)

#### 3. 로컬 LLM 폴백 (WebLLM)

```typescript
// src/utils/localLLM.ts
import { WebLLM } from '@mlc-ai/web-llm';

// VPS Gemma3 실패 시 로컬 LLM 사용
// 응답 속도: 1-2초 (로컬 처리)
```

**효과**: 네트워크 지연 없이 즉시 응답

---

## 📋 구현 우선순위

### Phase 1: 성능 개선 (즉시 필요)
1. ✅ **AI 대화 성능 개선** (무전기 방식, 스트리밍 응답)
2. ✅ **로컬 LLM 폴백** (WebLLM 통합)

### Phase 2: 학습 최적화 (1주일 내)
3. ✅ **RPPG 기반 동적 학습 스케줄링**
4. ✅ **음성 기반 메타인지 확신도 분석**

### Phase 3: 동기부여 강화 (2주일 내)
5. ✅ **수능 역추적 커리큘럼 매핑**
6. ✅ **체질별 페이스메이커 페르소나 매칭**

---

## 🚀 다음 단계

**주인님, 어떤 기능부터 구현할까요?**

1. **AI 대화 성능 개선** (무전기 방식, 스트리밍) - 가장 시급
2. **RPPG 기반 학습 스케줄링** - 생체 리듬 활용
3. **음성 확신도 분석** - 메타인지 강화
4. **수능 역추적 매핑** - 동기부여
5. **페르소나 매칭** - 개인화

**또는 모두 한 번에 구현할까요?** (Nitro 모드 활성화)

---

**작성일**: 2026-01-20  
**상태**: ✅ 현재 구현 상태 분석 완료

[Verified by Athena Auditor v1.2]

