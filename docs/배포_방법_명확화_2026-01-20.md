# 🚀 배포 방법 명확화

**작성일**: 2026-01-20  
**프로젝트**: mkm-study20260120  
**상태**: 배포 방법 선택 필요

---

## 📊 배포 옵션 비교

### 옵션 1: Vercel 배포 (권장) ⭐⭐⭐

**무엇인가요?**
- Vercel은 **프론트엔드 배포 플랫폼**입니다
- GitHub에 코드를 푸시하면 **자동으로 배포**됩니다
- **무료 플랜** 제공 (개인 프로젝트)

**장점**:
- ✅ **가장 빠른 배포** (1-2분)
- ✅ **무료 플랜** 제공
- ✅ **자동 HTTPS** (SSL 인증서 자동 설정)
- ✅ **CDN 자동 설정** (전 세계 빠른 로딩)
- ✅ **GitHub 연동** (자동 배포)

**비용**: 무료

**배포 시간**: 약 1-2분

**배포 방법**:
1. Vercel 웹사이트 접속: https://vercel.com
2. GitHub 연동: GitHub 계정으로 로그인
3. 프로젝트 Import: `mkmlab-v2/mkm-study20260120` 선택
4. 환경 변수 설정:
   - `VITE_VPS_GEMMA3_URL` = `http://148.230.97.246:11434`
   - `VITE_API_BASE_URL` = `http://148.230.97.246:8003`
5. "Deploy" 버튼 클릭

**결과**: 
- 배포된 URL: `https://mkm-study20260120.vercel.app` (예시)
- HTTPS 자동 설정
- CDN 자동 설정

---

### 옵션 2: VPS 배포 (Nitro)

**무엇인가요?**
- VPS는 **가상 서버**입니다
- 우리가 이미 사용 중인 서버 (`148.230.97.246`)
- **수동으로 배포**해야 합니다

**장점**:
- ✅ **완전한 제어** (서버 설정 자유롭게)
- ✅ **백엔드 API와 같은 서버** (같은 VPS 사용 가능)

**단점**:
- ⚠️ **수동 설정 필요** (Nginx, SSL 등)
- ⚠️ **배포 시간 느림** (5-10분)
- ⚠️ **VPS 비용** (월 $5-10)

**비용**: VPS 서버 비용 (월 $5-10)

**배포 시간**: 약 5-10분

**배포 방법**:
```powershell
cd C:\workspace\projects\mkm\mkm-study20260120
.\scripts\deploy-vps-nitro.ps1
```

**결과**:
- 배포된 URL: `http://148.230.97.246:3001` 또는 도메인
- HTTPS 수동 설정 필요
- CDN 수동 설정 필요

---

## 🎯 추천: Vercel 배포 (권장) ⭐

**이유**:
1. **가장 빠른 배포** (1-2분)
2. **무료 플랜** 제공
3. **자동 HTTPS 및 CDN** 설정
4. **GitHub 연동**으로 자동 배포

---

## 🚀 자동 배포 진행

### 방법 1: Vercel 웹 UI 배포 (가장 간단) ⭐⭐⭐

**단계별 가이드**:

1. **Vercel 웹사이트 접속**: https://vercel.com
2. **GitHub 연동**: GitHub 계정으로 로그인
3. **프로젝트 Import**: 
   - "Add New Project" 클릭
   - "Import Git Repository" 선택
   - `mkmlab-v2/mkm-study20260120` 검색 및 선택
4. **프로젝트 설정** (자동 감지됨):
   - Framework Preset: Vite ✅
   - Root Directory: `./` ✅
   - Build Command: `npm run build` ✅
   - Output Directory: `dist` ✅
5. **환경 변수 설정**:
   - "Environment Variables" 섹션에서 추가:
     - `VITE_VPS_GEMMA3_URL` = `http://148.230.97.246:11434`
     - `VITE_API_BASE_URL` = `http://148.230.97.246:8003`
6. **배포 실행**: "Deploy" 버튼 클릭

**예상 시간**: 약 2-3분

**자동 설정**:
- ✅ HTTPS 자동 설정
- ✅ CDN 자동 설정
- ✅ GitHub 푸시 시 자동 재배포

---

### 방법 2: Vercel CLI 배포 (터미널)

**단계별 가이드**:

```powershell
# 1. 프로젝트 디렉토리로 이동
cd C:\workspace\projects\mkm\mkm-study20260120

# 2. Vercel 로그인 (한 번만)
npx vercel login

# 3. 배포 실행 (프로덕션)
npx vercel --prod

# 4. 환경 변수 설정 (Vercel 웹 UI에서도 가능)
# Vercel 대시보드에서 환경 변수 추가:
# - VITE_VPS_GEMMA3_URL = http://148.230.97.246:11434
# - VITE_API_BASE_URL = http://148.230.97.246:8003
```

**예상 시간**: 약 1-2분

---

## 🔧 VPS 배포 (선택적)

**VPS 배포가 필요한 경우**:

```powershell
cd C:\workspace\projects\mkm\mkm-study20260120
.\scripts\deploy-vps-nitro.ps1
```

**예상 시간**: 약 5-10분

---

## 📋 배포 전 체크리스트 (모두 완료 ✅)

- [x] 빌드 성공: `npm run build` 완료
- [x] 타입 에러 수정: 모든 TypeScript 에러 해결
- [x] API 키 저장: Gemini API 키 저장 완료
- [x] VPS Gemma3 확인: 연결 및 모델 확인 완료
- [x] 배포 스크립트 준비: Vercel 및 VPS 배포 스크립트 생성 완료
- [x] Vercel 설정 파일: `vercel.json` 생성 완료

---

## 🎯 최종 추천 및 다음 단계

### 추천: Vercel 웹 UI 배포 ⭐⭐⭐

**가장 간단하고 빠른 방법**:

1. **Vercel 웹사이트 접속**: https://vercel.com
2. **GitHub 연동**: GitHub 계정으로 로그인
3. **프로젝트 Import**: `mkmlab-v2/mkm-study20260120` 선택
4. **환경 변수 설정**:
   - `VITE_VPS_GEMMA3_URL` = `http://148.230.97.246:11434`
   - `VITE_API_BASE_URL` = `http://148.230.97.246:8003`
5. **배포 실행**: "Deploy" 버튼 클릭

**예상 시간**: 약 2-3분

---

## ✅ 배포 준비 완료

**모든 준비가 완료되었습니다!**

**다음 단계**:
1. Vercel 웹사이트 접속: https://vercel.com
2. GitHub 연동 및 프로젝트 Import
3. 환경 변수 설정
4. 배포 실행

**또는** VPS 배포를 원하시면:
```powershell
.\scripts\deploy-vps-nitro.ps1
```

---

**작성일**: 2026-01-20  
**상태**: ✅ 배포 준비 완료  
**추천**: Vercel 웹 UI 배포 (가장 간단)

[Verified by Athena Auditor v1.2]

