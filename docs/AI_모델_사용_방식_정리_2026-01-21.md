# 🏛️ AI 모델 사용 방식 정리

**작성일**: 2026-01-21  
**목적**: Cursor IDE vs mkm-study 앱의 AI 모델 사용 방식 명확화  
**상태**: ✅ 정리 완료

---

## 🎯 핵심 정리

### 1. mkm-study 앱의 목적

**엔드유저**: 초등학교 6학년 ~ 중학교 3학년 학생

**용도**: 
- 학습 질문 답변 (수학, 영어)
- 학습 도우미 (튜터 역할)
- 생체 신호 기반 학습 최적화

**AI 모델 필요성**: ✅ **필수**
- 학생이 질문하면 답변해주는 튜터 역할
- Cursor IDE와는 별개

---

### 2. Cursor IDE vs mkm-study 앱

| 항목 | Cursor IDE | mkm-study 앱 |
|------|-----------|-------------|
| **사용자** | 개발자 (주인님) | 학생 (엔드유저) |
| **목적** | 코드 작성 도움 | 학습 질문 답변 |
| **AI 모델** | Cursor AI (자동 제안) | Ollama (명시적 호출) |
| **활용 방식** | 자동 (코드 작성 시) | 명시적 (마이크 버튼) |
| **환경** | 로컬 개발 환경 | 웹 앱 (Vercel 배포) |

**결론**: 완전히 별개의 시스템입니다.

---

### 3. 대화 모델의 용도

#### mkm-study 앱에서

**현재 구현**:
- 학생이 마이크 버튼을 눌러 질문
- AI가 답변 생성 (스트리밍)
- 학습 도우미 역할

**코드 위치**:
```typescript
// src/components/MKMStudyApp.tsx
// 마이크 버튼 클릭 → 음성 인식 → AI 답변
onMouseDown={() => {
  recognitionRef.current.start(); // 음성 인식 시작
}}
onMouseUp={() => {
  recognitionRef.current.stop(); // 음성 인식 종료
  // → answerQuestionStreaming() 호출
}}
```

**활용 방식**: ✅ **명시적 호출** (마이크 버튼 클릭)

---

#### 외부 서비스/상용화 시

**자체 대화 모델 사용**: ✅ **맞음**

**이유**:
- 데이터 주권 (외부 API 의존 없음)
- 비용 절감 (자체 서버 운영)
- 커스터마이징 가능 (학습 특화 모델)

**현재 구조**:
- 로컬 Ollama: `athena-merged-v1:latest` (주인님 철학 반영)
- VPS Ollama: `gemma3:4b` (폴백)

---

### 4. Aider 같은 코딩 모델

**현재 상태**: ❌ **프로젝트에 없음**

**이유**:
- mkm-study 앱은 **학생용 학습 앱**
- 코딩 모델은 필요 없음
- Cursor IDE에서 이미 사용 중 (별개)

**만약 필요하다면**:
- 별도 프로젝트로 분리
- 또는 개발자 도구로 통합 (선택적)

---

## 📋 현재 AI 모델 활용 방식

### 명시적 호출 (현재 구현)

**사용자 액션**:
1. 학생이 "질문" 탭 클릭
2. 마이크 버튼을 길게 누름 (Push-to-Talk)
3. 질문 말하기
4. 버튼 놓기
5. AI 답변 생성 (스트리밍)

**코드 흐름**:
```typescript
// 1. 마이크 버튼 클릭
onMouseDown → recognitionRef.current.start()

// 2. 음성 인식 완료
recognition.onresult → transcript 받음

// 3. AI 답변 요청
answerQuestionStreaming(transcript, currentState, subject)

// 4. 스트리밍 응답 표시
for await (const chunk of answerQuestionStreaming(...)) {
  setAnswer(prev => prev + chunk); // 실시간 업데이트
}
```

**활용 방식**: ✅ **명시적 호출** (사용자가 직접 버튼 클릭)

---

### 자동 활용 (미구현, 가능)

**예시 시나리오**:
- 학습 중 어려운 개념 감지 → 자동으로 설명 제안
- RPPG로 스트레스 감지 → 자동으로 쉬운 문제 제안
- 메타인지 확신도 낮음 감지 → 자동으로 재설명

**구현 방법**:
```typescript
// 자동 활용 예시 (미구현)
useEffect(() => {
  if (confidenceAnalysis && confidenceAnalysis.confidence < 0.5) {
    // 확신도 낮으면 자동으로 재설명 제안
    autoExplainConcept(currentTopic);
  }
}, [confidenceAnalysis]);
```

**현재 상태**: ❌ **구현 안 됨** (명시적 호출만)

---

## 🚀 외부 서비스/상용화 시 전략

### 자체 대화 모델 사용

**현재 구조**:
- 로컬 Ollama: `athena-merged-v1:latest` (우선)
- VPS Ollama: `gemma3:4b` (폴백)

**상용화 시**:
- VPS에 `athena-merged-v1:latest` 배포
- 또는 더 큰 모델 사용 (13B, 70B 등)
- 자체 서버 운영 (데이터 주권)

**장점**:
- ✅ 외부 API 의존 없음
- ✅ 비용 절감
- ✅ 커스터마이징 가능

---

## ✅ 최종 정리

### 질문 1: "Cursor IDE에서 사용 중이므로 대화 모델이 우선 필요 없다?"

**답변**: ❌ **아님**

- mkm-study 앱은 **학생용 학습 앱** (별개)
- 학생이 질문하면 답변해주는 **튜터 역할** 필요
- Cursor IDE는 개발 도구 (별개)

---

### 질문 2: "외부 서비스나 상용화 시 자체 대화 모델을 사용한다는 것인가?"

**답변**: ✅ **맞음**

- 현재: 로컬/VPS Ollama 사용 (자체 모델)
- 상용화: 자체 서버에 모델 배포
- 외부 API 의존 없음 (데이터 주권)

---

### 질문 3: "Aider 같은 코딩 모델도 있는가?"

**답변**: ❌ **없음**

- mkm-study 앱은 학생용 학습 앱
- 코딩 모델 불필요
- Cursor IDE에서 이미 사용 중 (별개)

---

### 질문 4: "자동 활용인가? 명시적으로 해야 하나?"

**답변**: ✅ **명시적 호출** (현재)

- 마이크 버튼 클릭 → 질문 → 답변
- 자동 활용은 미구현 (가능하지만 현재는 안 함)

**자동 활용 예시** (미구현):
- 확신도 낮으면 자동 재설명
- 스트레스 높으면 자동 쉬운 문제 제안

---

## 🎯 결론

**mkm-study 앱의 AI 모델**:
- ✅ **필수** (학생 질문 답변용)
- ✅ **명시적 호출** (마이크 버튼)
- ✅ **자체 모델 사용** (로컬/VPS Ollama)
- ❌ **Aider 없음** (코딩 모델 불필요)

**Cursor IDE**:
- 별개 시스템 (개발 도구)
- Cursor AI 자동 제안 사용

---

**작성일**: 2026-01-21  
**상태**: ✅ 정리 완료

[Verified by Athena Auditor v1.2]

